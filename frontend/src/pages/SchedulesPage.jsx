import React, { useState, useEffect, useCallback, useMemo } from 'react';
import api from '../utils/api';
import { fmtDay, fmtISO, fmtMoney, getWeekDates, getMonthDates, DAYS, RATE_KM } from '../utils/helpers';
import { Avatar, Modal } from '../components/UI';
import { ChevronLeft, ChevronRight, Plus, Send, Calendar, Search, FileText, MapPin, DollarSign, Truck, CalendarRange, Trash2 } from 'lucide-react';

const MONTHS_FULL = ['Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet', 'Août', 'Septembre', 'Octobre', 'Novembre', 'Décembre'];
const MONTHS_SHORT = ['Jan', 'Fév', 'Mar', 'Avr', 'Mai', 'Jun', 'Jul', 'Aoû', 'Sep', 'Oct', 'Nov', 'Déc'];
const LOCATIONS = ["CH de Rouyn-Noranda", "Villa des Brises", "CLSC de Grande-Vallée-Gaspésie", "CSSS de Sept-Iles", "Dispensaire de Baie-Johan-Beetz", "Bureau Soins Expert Plus", "Centre Multi Services de Havre-Saint-Pierre", "CISSS des Îles", "CHSLD de New Carlisle", "La Balise (Bonaventure)", "Hôpital de Matane", "CHSLD Ville-Marie (Abitibi)", "Hôpital de Notre-Dame-du-Lac", "Hôpital de Gaspésie", "Centre de santé Basse Côte-Nord", "CH régional du Grand-Portage", "Résidence Plaisance - CISSS des Îles", "Forestville, QC", "CHSLD de Sept-Îles", "CHSLD – Pavillon Eudore-Labrie", "CHSLD de Chauffailles", "CHSLD SEPT ILES", "CHSLD de Senneterre", "CHSLD de Cap-Chat", "CHSLD Mgr-Ross", "CHSLD RIMOUSKI", "CHSLD BASQUES", "Centre de Santé et de Services sociaux de Sept-Iles", "Centre hospitalier régional du Grand-Portage", "Centre de santé de la Basse Côte-Nord - Blanc sablon", "Hôpital régional de Rimouski", "CRSSS de la Baie-James", "CISSS de la Gaspésie", "CISSS de la Côte-Nord", "Baie-Comeau", "Les Escoumins", "AIDE A DOMICILE", "Centre Jeunesse Sept Iles", "CHSLD St-Eusèbe", "CHSLD Villa Maria", "CHSLD de Val-d'Or", "CH MARIA // CISSS de la Gaspésie", "CRDI AMOS", "CHSLD de Rigaud", "CHSLD Désy"];
const PAUSE_OPTIONS = [{ value: '0', label: 'Aucune' }, { value: '0.25', label: '15 min' }, { value: '0.5', label: '30 min' }, { value: '0.75', label: '45 min' }, { value: '1', label: '1 heure' }, { value: 'custom', label: 'Personnalisée...' }];
const RECURRENCE_OPTIONS = [{ value: 'once', label: 'Une seule fois' }, { value: 'daily', label: 'Chaque jour' }, { value: 'weekdays', label: 'Lundi au vendredi' }, { value: 'custom', label: 'Jours spécifiques...' }];
const TPS_RATE = 0.05;
const TVQ_RATE = 0.09975;

function calcHours(start, end, pause = 0) {
  if (!start || !end) return 0;
  const [sh, sm] = start.split(':').map(Number);
  const [eh, em] = end.split(':').map(Number);
  let startMin = sh * 60 + sm, endMin = eh * 60 + em;
  if (endMin <= startMin) endMin += 24 * 60;
  return Math.max(0, Math.round(((endMin - startMin) / 60 - pause) * 100) / 100);
}
function getRecurrenceDates(startDate, mode, endDate, customDays = []) {
  if (mode === 'once') return [startDate];
  if (!endDate) return [startDate];
  const start = new Date(startDate + 'T12:00:00');
  const end = new Date(endDate + 'T12:00:00');
  if (end < start) return [startDate];
  const dates = [];
  const cur = new Date(start);
  while (cur <= end) {
    const iso = fmtISO(cur);
    const dow = cur.getDay();
    if (mode === 'daily') dates.push(iso);
    else if (mode === 'weekdays' && dow >= 1 && dow <= 5) dates.push(iso);
    else if (mode === 'custom' && customDays.includes(dow)) dates.push(iso);
    cur.setDate(cur.getDate() + 1);
    if (dates.length > 60) break;
  }
  return dates.length ? dates : [startDate];
}
function normalizeTimeForInput(value) {
  if (!value) return '';
  const raw = String(value).trim();
  const m = raw.match(/^(\d{1,2}):(\d{2})(?::\d{2})?$/);
  if (!m) return '';
  return `${String(Number(m[1])).padStart(2, '0')}:${m[2]}`;
}
function pauseHoursToMinutes(value) { return Math.round(Number(value || 0) * 60); }
function pauseMinutesToHours(value) { return Math.round((Number(value || 0) / 60) * 100) / 100; }
function computeAccommodationEstimate(accommodation, shifts) {
  const workedDates = [...new Set((shifts || []).map(s => s.date).filter(Boolean))].sort();
  const fullSpanWorked = workedDates.filter(d => d >= accommodation.start_date && d <= accommodation.end_date);
  const fullSpanCount = fullSpanWorked.length;
  const totalCost = Number(accommodation.total_cost || 0);
  const fallbackCount = Number(accommodation.days_worked || 0);
  const denominator = fullSpanCount || fallbackCount || 1;
  const costPerWorkedDay = totalCost > 0 ? totalCost / denominator : Number(accommodation.cost_per_day || 0);
  const periodCount = fullSpanWorked.length;
  return { days: periodCount, costPerDay: costPerWorkedDay, amount: periodCount * costPerWorkedDay };
}

export default function SchedulesPage({ toast, onNavigate }) {
  const [schedules, setSchedules] = useState([]), [employees, setEmployees] = useState([]), [clients, setClients] = useState([]), [viewMode, setViewMode] = useState('week'), [selectedDate, setSelectedDate] = useState(new Date()), [filterText, setFilterText] = useState(''), [modal, setModal] = useState(null), [empDetail, setEmpDetail] = useState(null), [loading, setLoading] = useState(true), [approvals, setApprovals] = useState([]), [expandedEmp, setExpandedEmp] = useState(null), [reviewDraft, setReviewDraft] = useState({ approvedHours: 0, notes: '' }), [reviewAttachments, setReviewAttachments] = useState([]), [currentReview, setCurrentReview] = useState(null), [currentInvoice, setCurrentInvoice] = useState(null), [billingLoading, setBillingLoading] = useState(false), [bulkLoading, setBulkLoading] = useState(false);

  const reload = useCallback(async () => {
    try {
      const [scheds, emps, cls, reviews] = await Promise.all([api.getSchedules(), api.getEmployees(), api.getClients(), api.getScheduleReviews().catch(() => [])]);
      setSchedules(scheds || []); setEmployees(emps || []); setClients(cls || []); setApprovals(reviews || []);
    } catch (err) { toast?.('Erreur: ' + err.message); } finally { setLoading(false); }
  }, [toast]);
  useEffect(() => { reload(); }, [reload]);

  const viewDates = useMemo(() => viewMode === 'week' ? getWeekDates(selectedDate, 0) : viewMode === 'month' ? getMonthDates(selectedDate) : [], [viewMode, selectedDate]);
  const viewISOs = useMemo(() => viewDates.map(fmtISO), [viewDates]);
  const activeEmpIds = useMemo(() => viewMode === 'year' ? [...new Set(schedules.filter(s => s.date.startsWith(String(selectedDate.getFullYear()))).map(s => s.employee_id))] : [...new Set(schedules.filter(s => viewISOs.includes(s.date)).map(s => s.employee_id))], [schedules, viewISOs, viewMode, selectedDate]);
  const activeEmps = useMemo(() => { let emps = employees.filter(e => activeEmpIds.includes(e.id)).sort((a, b) => a.name.localeCompare(b.name)); if (filterText) { const q = filterText.toLowerCase(); emps = emps.filter(e => (e.name.toLowerCase() + ' ' + (e.position || '').toLowerCase()).includes(q)); } return emps; }, [employees, activeEmpIds, filterText]);
  const otherEmps = useMemo(() => employees.filter(e => !activeEmpIds.includes(e.id)).sort((a, b) => a.name.localeCompare(b.name)), [employees, activeEmpIds]);
  const navigate = (dir) => setSelectedDate(d => { const n = new Date(d); if (viewMode === 'week') n.setDate(n.getDate() + dir * 7); else if (viewMode === 'month') n.setMonth(n.getMonth() + dir); else n.setFullYear(n.getFullYear() + dir); return n; });
  const goToDate = (dateStr) => { if (dateStr) setSelectedDate(new Date(dateStr + 'T12:00:00')); };
  const periodLabel = useMemo(() => viewMode === 'week' && viewDates.length >= 7 ? `${fmtDay(viewDates[0])} — ${fmtDay(viewDates[6])}` : viewMode === 'month' ? `${MONTHS_FULL[selectedDate.getMonth()]} ${selectedDate.getFullYear()}` : `Année ${selectedDate.getFullYear()}`, [viewMode, viewDates, selectedDate]);
  const getWeekStart = () => (viewMode === 'week' && viewDates.length >= 7 ? fmtISO(viewDates[0]) : null);
  const getWeekEnd = () => (viewMode === 'week' && viewDates.length >= 7 ? fmtISO(viewDates[6]) : null);
  const getWeekApprovalStatus = (employeeId, clientId) => approvals.find(a => a.employee_id === employeeId && a.client_id === clientId && a.week_start === getWeekStart());
  const getEmployeeClientIds = (employee, periodShifts) => { const ids = [...new Set(periodShifts.map(s => s.client_id).filter(Boolean))]; if (ids.length) return ids; if (employee?.client_id) return [employee.client_id]; return []; };
  const getClientWeekShifts = (employeeId, clientId, fallbackClientId = null) => schedules.filter(s => s.employee_id === employeeId && viewISOs.includes(s.date) && (s.client_id === clientId || (!s.client_id && fallbackClientId && fallbackClientId === clientId)));
  const getClientWeekHours = (employeeId, clientId, fallbackClientId = null) => getClientWeekShifts(employeeId, clientId, fallbackClientId).reduce((sum, s) => sum + (Number(s.hours) || 0), 0);
  const openAdd = (employeeId, date) => { const emp = employees.find(e => e.id === employeeId); setModal({ type: 'add', data: { employeeId: employeeId || '', date: date || fmtISO(viewDates[0] || new Date()), start: '07:00', end: '15:00', pause: 0.75, pauseMode: '0.75', pauseCustomMin: '', hours: 7.25, location: LOCATIONS[0], billableRate: emp?.rate || 0, status: 'draft', notes: '', clientId: emp?.client_id || 0, mandatStart: '', mandatEnd: '', km: 0, deplacement: 0, autreDep: 0, depNote: '', recurrence: 'once', recurrenceEnd: '', customDays: [] } }); };
  const openEdit = (shift) => { const pauseVal = shift.pause || 0; const isCustom = ![0, .25, .5, .75, 1].includes(pauseVal) && pauseVal > 0; setModal({ type: 'edit', data: { ...shift, employeeId: shift.employee_id, billableRate: shift.billable_rate, clientId: shift.client_id || 0, mandatStart: shift.mandat_start || '', mandatEnd: shift.mandat_end || '', autreDep: shift.autre_dep || 0, depNote: shift.dep_note || '', pauseMode: isCustom ? 'custom' : String(pauseVal), pauseCustomMin: isCustom ? String(Math.round(pauseVal * 60)) : '' } }); };
  const saveShift = async () => { const d = modal.data; if (!d.employeeId || !d.date) { toast?.('Remplir employé et date'); return; } try { if (modal.type === 'add') { const payload = { employee_id: Number(d.employeeId), date: d.date, start: d.start, end: d.end, hours: d.hours, pause: d.pause, location: d.location, billable_rate: d.billableRate || 0, status: d.status, notes: d.notes, client_id: d.clientId || null, km: d.km || 0, deplacement: d.deplacement || 0, autre_dep: d.autreDep || 0, mandat_start: d.mandatStart || null, mandat_end: d.mandatEnd || null, recurrence: d.recurrence, recurrence_end: d.recurrenceEnd || null, recurrence_days: d.recurrence === 'custom' ? d.customDays : null }; const result = await api.createSchedule(payload); toast?.(`${result.created || 1} quart(s) ajouté(s)`); } else { await api.updateSchedule(d.id, { start: d.start, end: d.end, hours: d.hours, pause: d.pause, location: d.location, billable_rate: d.billableRate || 0, status: d.status, notes: d.notes, km: d.km || 0, deplacement: d.deplacement || 0, autre_dep: d.autreDep || 0, mandat_start: d.mandatStart || null, mandat_end: d.mandatEnd || null }); toast?.('Quart modifié'); } setModal(null); reload(); } catch (err) { toast?.('Erreur: ' + err.message); } };
  const deleteShift = async () => { try { await api.deleteSchedule(modal.data.id); toast?.('Quart supprimé'); setModal(null); reload(); } catch (err) { toast?.('Erreur: ' + err.message); } };
  const publishAll = async () => { try { const res = await api.publishAll(); toast?.(`${res.published} quart(s) publié(s)`); reload(); } catch (err) { toast?.('Erreur: ' + err.message); } };

  const loadReviewPanel = async (empId, clientId, fallbackClientId = null) => { const ws = getWeekStart(), we = getWeekEnd(); if (!ws || !we || !empId || !clientId) return; setBillingLoading(true); setExpandedEmp({ empId, clientId, fallbackClientId }); setCurrentReview(null); setCurrentInvoice(null); setReviewAttachments([]); const plannedHours = Number(getClientWeekHours(empId, clientId, fallbackClientId).toFixed(2)); setReviewDraft({ approvedHours: plannedHours, notes: '' }); try { const [reviews, invoices] = await Promise.all([api.getScheduleReviews({ employee_id: empId, client_id: clientId, week_start: ws }), api.getInvoices({ employee_id: empId, client_id: clientId, period_start: ws, period_end: we })]); const review = (reviews || [])[0] || null; const invoice = (invoices || [])[0] || null; setCurrentReview(review); setCurrentInvoice(invoice); if (review?.id) { setReviewDraft({ approvedHours: Number(review.approved_hours || plannedHours), notes: review.notes || '' }); const att = await api.getScheduleReviewAttachments(review.id).catch(() => []); setReviewAttachments(att || []); } } catch (err) { toast?.('Erreur: ' + err.message); } finally { setBillingLoading(false); } };
  const toggleBillingPanel = async (empId, clientId, fallbackClientId = null) => { if (!empId || !clientId) return; if (expandedEmp?.empId === empId && expandedEmp?.clientId === clientId) { setExpandedEmp(null); return; } await loadReviewPanel(empId, clientId, fallbackClientId); };

  const saveReview = async (empId, clientId, fallbackClientId = null, approve = false) => { const ws = getWeekStart(); if (!ws || !empId || !clientId) return; try { setBillingLoading(true); const approvedHours = Number(reviewDraft?.approvedHours || getClientWeekHours(empId, clientId, fallbackClientId) || 0); const notes = reviewDraft?.notes || ''; let review = approve ? await api.approveReviewedWeek({ employee_id: empId, client_id: clientId, week_start: ws, approved_hours: approvedHours, notes }) : await api.reviewWeek({ employee_id: empId, client_id: clientId, week_start: ws, approved_hours: approvedHours, notes }); if (approve && (!review || review.status !== 'approved')) review = { ...(review || {}), employee_id: empId, client_id: clientId, week_start: ws, status: 'approved' }; setCurrentReview(review || null); setApprovals(prev => { const rest = prev.filter(a => !(a.employee_id === empId && a.client_id === clientId && a.week_start === ws)); return review ? [review, ...rest] : rest; }); if (review?.id) { const att = await api.getScheduleReviewAttachments(review.id).catch(() => []); setReviewAttachments(att || []); } if (approve) await reload(); toast?.(approve ? 'Heures approuvées' : 'Révision enregistrée'); } catch (err) { toast?.('Erreur: ' + err.message); } finally { setBillingLoading(false); } };
  const revokeWeek = async (employeeId, clientId) => { const ws = getWeekStart(); if (!ws || !employeeId || !clientId) return; try { await api.revokeReviewedWeek({ employee_id: employeeId, client_id: clientId, week_start: ws }); toast?.('Approbation révoquée'); reload(); if (expandedEmp?.empId === employeeId && expandedEmp?.clientId === clientId) setCurrentReview(r => r ? { ...r, status: 'rejected' } : r); } catch (err) { toast?.('Erreur: ' + err.message); } };
  const handleReviewAttachment = async (e, empId, clientId, fallbackClientId = null) => { const file = e.target.files?.[0]; if (!file || !empId || !clientId) return; try { let review = currentReview; if (!review?.id) { review = await api.reviewWeek({ employee_id: empId, client_id: clientId, week_start: getWeekStart(), approved_hours: Number(reviewDraft?.approvedHours || getClientWeekHours(empId, clientId, fallbackClientId) || 0), notes: reviewDraft?.notes || '' }); setCurrentReview(review || null); } if (!review?.id) throw new Error('Révision introuvable'); await api.uploadScheduleReviewAttachment(review.id, file, 'justificatif', file.name); const att = await api.getScheduleReviewAttachments(review.id); setReviewAttachments(att || []); toast?.('Justificatif ajouté'); } catch (err) { toast?.('Erreur: ' + err.message); } finally { e.target.value = ''; } };
  const deleteReviewAttachment = async (attId) => { if (!currentReview?.id || !attId) return; try { await api.deleteScheduleReviewAttachment(currentReview.id, attId); const att = await api.getScheduleReviewAttachments(currentReview.id); setReviewAttachments(att || []); toast?.('Justificatif supprimé'); } catch (err) { toast?.('Erreur: ' + err.message); } };
  const generateInvoice = async (empId, clientId) => { const ws = getWeekStart(), we = getWeekEnd(); if (!ws || !we || !empId || !clientId) return; try { setBillingLoading(true); const result = await api.generateFromSchedules({ employee_id: empId, client_id: clientId, period_start: ws, period_end: we }); setCurrentInvoice(result || null); toast?.(`Facture ${result.number} générée`); } catch (err) { toast?.('Erreur: ' + err.message); } finally { setBillingLoading(false); } };
  const generateAllApproved = async () => { const ws = getWeekStart(), we = getWeekEnd(); if (!ws || !we) return; try { setBulkLoading(true); const result = await api.generateAllApprovedInvoices({ period_start: ws, period_end: we }); toast?.(`${result.count || 0} facture(s) approuvée(s) générée(s)`); if ((result.count || 0) > 0 && onNavigate) onNavigate('invoices'); } catch (err) { toast?.('Erreur: ' + err.message); } finally { setBulkLoading(false); } };

  if (loading) return <div style={{ padding: 40, textAlign: 'center', color: 'var(--text3)' }}>Chargement des horaires...</div>;

  return <>
    <div className="page-header"><h1 className="page-title"><Calendar size={22} style={{ marginRight: 8, verticalAlign: 'text-bottom', color: 'var(--brand)' }} />Horaires</h1><div style={{ display: 'flex', gap: 8 }}>{viewMode === 'week' && <button className="btn btn-outline btn-sm" onClick={generateAllApproved} disabled={bulkLoading}><FileText size={13} /> {bulkLoading ? 'Génération…' : 'Générer toutes les factures approuvées'}</button>}<button className="btn btn-outline btn-sm" onClick={publishAll}><Send size={13} /> Publier tout</button><button className="btn btn-primary btn-sm" onClick={() => openAdd(employees[0]?.id, fmtISO(viewDates[0] || new Date()))}><Plus size={14} /> Ajouter</button></div></div>
    <div style={{ display: 'flex', flexDirection: 'column', gap: 10, marginBottom: 16 }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: 8 }}><div style={{ display: 'flex', alignItems: 'center', gap: 6 }}><button className="btn btn-outline btn-sm" onClick={() => navigate(-1)}><ChevronLeft size={16} /></button><span style={{ fontSize: 13, fontWeight: 600, minWidth: 230, textAlign: 'center' }}>{periodLabel}</span><button className="btn btn-outline btn-sm" onClick={() => navigate(1)}><ChevronRight size={16} /></button></div><div style={{ display: 'flex', alignItems: 'center', gap: 6 }}><div style={{ display: 'flex', gap: 2, background: 'var(--surface2)', borderRadius: 8, padding: 2 }}>{['week', 'month', 'year'].map(m => <button key={m} className="btn btn-sm" style={{ background: viewMode === m ? 'var(--brand)' : 'transparent', color: viewMode === m ? '#fff' : 'var(--text2)', border: 'none' }} onClick={() => setViewMode(m)}>{{ week: 'Semaine', month: 'Mois', year: 'Année' }[m]}</button>)}</div><input type="date" className="input" style={{ width: 150, padding: '5px 8px', fontSize: 12 }} value={fmtISO(selectedDate)} onChange={e => goToDate(e.target.value)} /><button className="btn btn-outline btn-sm" onClick={() => setSelectedDate(new Date())}>Aujourd'hui</button></div></div><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: 8 }}><div style={{ position: 'relative', maxWidth: 300 }}><Search size={14} style={{ position: 'absolute', left: 10, top: '50%', transform: 'translateY(-50%)', color: 'var(--text3)' }} /><input className="input" style={{ paddingLeft: 32, padding: '7px 12px 7px 32px', fontSize: 12 }} placeholder="Rechercher un employé..." value={filterText} onChange={e => setFilterText(e.target.value)} /></div><div style={{ fontSize: 12, color: 'var(--text3)' }}>{schedules.length} quarts · {employees.length} employés</div></div></div>
    {viewMode === 'year' ? <YearView schedules={schedules} employees={activeEmps} selectedDate={selectedDate} /> : <div className="schedule-grid"><table><thead><tr><th>Employé</th>{viewDates.map((d, i) => <th key={i}>{fmtDay(d)}</th>)}<th style={{ minWidth: 120, background: 'var(--brand-xl)' }}>Total</th></tr></thead><tbody>{activeEmps.map(e => { const periodShifts = schedules.filter(s => s.employee_id === e.id && viewISOs.includes(s.date)); const totalHrs = periodShifts.reduce((sum, s) => sum + s.hours, 0); const totalKm = periodShifts.reduce((sum, s) => sum + (s.km || 0), 0); const totalDep = periodShifts.reduce((sum, s) => sum + (s.deplacement || 0) + (s.autre_dep || 0), 0); const totalFrais = totalDep + totalKm * RATE_KM; const empClientIds = getEmployeeClientIds(e, periodShifts); return <React.Fragment key={e.id}><tr><td><div style={{ display: 'flex', alignItems: 'center', gap: 8 }}><Avatar name={e.name} size={30} /><div><div style={{ fontWeight: 600, fontSize: 12 }}>{e.name}</div><div style={{ fontSize: 10, color: 'var(--text3)' }}>{(e.position || '').slice(0, 24)}</div></div></div></td>{viewDates.map((d, i) => { const iso = fmtISO(d); const shifts = schedules.filter(s => s.employee_id === e.id && s.date === iso); return <td key={i}>{shifts.map(s => <ShiftPill key={s.id} shift={s} onClick={() => openEdit(s)} />)}{shifts.length === 0 && <div style={{ opacity: .2, textAlign: 'center', fontSize: 16, lineHeight: '30px', color: 'var(--brand)' }}>+</div>}</td>; })}<td style={{ minWidth: 160, textAlign: 'center', verticalAlign: 'middle', background: 'var(--brand-xl)', borderLeft: '2px solid var(--border)' }}><div style={{ fontWeight: 700, fontSize: 13, color: 'var(--brand)' }}>{totalHrs.toFixed(1)}h</div>{totalFrais > 0 && <div style={{ fontSize: 10, color: 'var(--purple)', marginTop: 2 }}>{fmtMoney(totalFrais)} frais</div>}{totalKm > 0 && <div style={{ fontSize: 9, color: 'var(--text3)' }}>{totalKm} km</div>}{viewMode === 'week' && empClientIds.map(cid => { const appr = getWeekApprovalStatus(e.id, cid); const isApproved = appr && appr.status === 'approved'; const cl = clients.find(c => c.id === cid); return <div key={cid} style={{ display: 'flex', flexDirection: 'column', gap: 2, marginTop: 6 }}><div style={{ fontSize: 9, color: 'var(--text3)' }}>{(cl?.name || 'Client').slice(0, 18)}</div><button style={{ display: 'block', width: '100%', padding: '2px 6px', fontSize: 9, fontWeight: 600, border: 'none', borderRadius: 4, cursor: 'pointer', background: isApproved ? '#28A745' : '#FFC107', color: isApproved ? '#fff' : '#000' }} onClick={() => isApproved ? revokeWeek(e.id, cid) : saveReview(e.id, cid, e.client_id || null, true)}>{isApproved ? '✓ Approuvé' : 'Approuver heures'}</button><button style={{ display: 'block', width: '100%', padding: '2px 6px', fontSize: 9, fontWeight: 500, border: '1px solid var(--border)', borderRadius: 4, cursor: 'pointer', background: expandedEmp?.empId === e.id && expandedEmp?.clientId === cid ? 'var(--brand)' : 'var(--surface)', color: expandedEmp?.empId === e.id && expandedEmp?.clientId === cid ? '#fff' : 'var(--text2)' }} onClick={() => toggleBillingPanel(e.id, cid, e.client_id || null)}>📋 {expandedEmp?.empId === e.id && expandedEmp?.clientId === cid ? 'Fermer' : 'Détails'}</button></div>; })}</td></tr>{viewMode === 'week' && expandedEmp?.empId === e.id && empClientIds.includes(expandedEmp?.clientId) && <tr><td colSpan={viewDates.length + 2} style={{ padding: 0 }}><div style={{ background: '#f0f9fa', borderTop: '2px solid var(--brand)', borderBottom: '2px solid var(--brand)', padding: '14px 18px' }}>{billingLoading ? <div style={{ textAlign: 'center', padding: 20, color: 'var(--text3)' }}>Chargement...</div> : <ApprovalPanel employee={e} client={clients.find(c => c.id === expandedEmp.clientId)} shifts={getClientWeekShifts(e.id, expandedEmp.clientId, expandedEmp?.fallbackClientId || e.client_id || null)} reviewDraft={reviewDraft} setReviewDraft={setReviewDraft} currentReview={currentReview} currentInvoice={currentInvoice} reviewAttachments={reviewAttachments} onSave={() => saveReview(e.id, expandedEmp.clientId, expandedEmp?.fallbackClientId || e.client_id || null, false)} onApprove={() => saveReview(e.id, expandedEmp.clientId, expandedEmp?.fallbackClientId || e.client_id || null, true)} onRevoke={() => revokeWeek(e.id, expandedEmp.clientId)} onGenerateInvoice={() => generateInvoice(e.id, expandedEmp.clientId)} onUpload={(ev) => handleReviewAttachment(ev, e.id, expandedEmp.clientId, expandedEmp?.fallbackClientId || e.client_id || null)} onDeleteAttachment={deleteReviewAttachment} onGoInvoices={() => onNavigate && onNavigate('invoices')} onRefreshParent={reload} />}</div></td></tr>}</React.Fragment>; })}</tbody></table></div>}
  </>;
}

function ApprovalPanel({ employee, client, shifts, reviewDraft, setReviewDraft, currentReview, currentInvoice, reviewAttachments, onSave, onApprove, onRevoke, onGenerateInvoice, onUpload, onDeleteAttachment, onGoInvoices, onRefreshParent }) {
  const [editableShifts, setEditableShifts] = useState([]), [savingShiftId, setSavingShiftId] = useState(null), [accommodations, setAccommodations] = useState([]), [loadingAccommodations, setLoadingAccommodations] = useState(false), [accomForm, setAccomForm] = useState({ total_cost: '', start_date: '', end_date: '', notes: '' }), [savingAccommodation, setSavingAccommodation] = useState(false);
  useEffect(() => { setEditableShifts((shifts || []).map(s => ({ ...s, start: normalizeTimeForInput(s.start), end: normalizeTimeForInput(s.end), other_dep: s.autre_dep ?? s.other_dep ?? 0, pause_minutes: pauseHoursToMinutes(s.pause), is_new: false }))); }, [shifts]);
  const loadAccommodations = useCallback(async () => { if (!employee?.id || !(shifts || []).length) { setAccommodations([]); return; } try { setLoadingAccommodations(true); const all = await api.getAccommodations(); const weekStart = shifts.map(s => s.date).sort()[0], weekEnd = shifts.map(s => s.date).sort().slice(-1)[0]; setAccommodations((all || []).filter(a => a.employee_id === employee.id && a.start_date <= weekEnd && a.end_date >= weekStart)); } catch { setAccommodations([]); } finally { setLoadingAccommodations(false); } }, [employee, shifts]);
  useEffect(() => { loadAccommodations(); }, [loadAccommodations]);
  const updateEditableShift = (id, field, value) => setEditableShifts(prev => prev.map(s => { if (s.id !== id) return s; const next = { ...s, [field]: value }; if (field === 'start' || field === 'end' || field === 'pause_minutes') { next.pause = pauseMinutesToHours(next.pause_minutes); next.hours = calcHours(next.start, next.end, Number(next.pause || 0)); } return next; }));
  const addQuickShiftRow = () => { const baseDate = shifts?.[0]?.date || fmtISO(new Date()); setEditableShifts(prev => [...prev, { id: `new-${Date.now()}-${prev.length}`, is_new: true, employee_id: employee.id, client_id: client?.id || null, date: baseDate, start: '07:00', end: '15:00', pause: 0, pause_minutes: 0, hours: 8, km: 0, deplacement: 0, other_dep: 0, notes: '', billable_rate: Number(employee?.rate || 0), status: 'published', location: '' }]); };
  const saveShiftLine = async (shift) => { try { setSavingShiftId(shift.id); if (shift.is_new) await api.createSchedule({ employee_id: employee.id, client_id: client?.id || null, date: shift.date, start: normalizeTimeForInput(shift.start), end: normalizeTimeForInput(shift.end), pause: Number(shift.pause || 0), hours: Number(shift.hours || 0), km: Number(shift.km || 0), deplacement: Number(shift.deplacement || 0), autre_dep: Number(shift.other_dep || 0), notes: shift.notes || '', billable_rate: Number(shift.billable_rate || employee?.rate || 0), status: 'published', location: shift.location || '' }); else await api.updateSchedule(shift.id, { start: normalizeTimeForInput(shift.start), end: normalizeTimeForInput(shift.end), pause: Number(shift.pause || 0), hours: Number(shift.hours || 0), km: Number(shift.km || 0), deplacement: Number(shift.deplacement || 0), autre_dep: Number(shift.other_dep || 0), notes: shift.notes || '' }); onRefreshParent?.(); } finally { setSavingShiftId(null); } };
  const saveQuickAccommodation = async () => { if (!accomForm.total_cost || !accomForm.start_date || !accomForm.end_date) return; try { setSavingAccommodation(true); await api.createAccommodation({ employee_id: employee.id, total_cost: Number(accomForm.total_cost || 0), start_date: accomForm.start_date, end_date: accomForm.end_date, days_worked: 0, cost_per_day: 0, notes: accomForm.notes || '' }); setAccomForm({ total_cost: '', start_date: '', end_date: '', notes: '' }); await loadAccommodations(); } finally { setSavingAccommodation(false); } };
  const accommodationEstimates = useMemo(() => (accommodations || []).map(a => ({ ...a, estimate: computeAccommodationEstimate(a, editableShifts) })), [accommodations, editableShifts]);
  const effectiveRate = Number(employee?.rate || editableShifts.find(s => Number(s.billable_rate || 0))?.billable_rate || 0);
  const totals = useMemo(() => { const service = editableShifts.reduce((sum, s) => sum + Number(s.hours || 0) * effectiveRate, 0), km = editableShifts.reduce((sum, s) => sum + Number(s.km || 0) * RATE_KM, 0), dep = editableShifts.reduce((sum, s) => sum + Number(s.deplacement || 0) * effectiveRate, 0), autres = editableShifts.reduce((sum, s) => sum + Number(s.other_dep || 0), 0), accom = accommodationEstimates.reduce((sum, a) => sum + Number(a.estimate.amount || 0), 0), subtotal = service + km + dep + autres + accom, includeTax = !client?.tax_exempt, tps = includeTax ? subtotal * TPS_RATE : 0, tvq = includeTax ? subtotal * TVQ_RATE : 0; return { service, km, dep, autres, accom, subtotal, tps, tvq, total: subtotal + tps + tvq }; }, [editableShifts, accommodationEstimates, effectiveRate, client]);
  const plannedHours = editableShifts.reduce((sum, s) => sum + (Number(s.hours) || 0), 0), totalKm = editableShifts.reduce((sum, s) => sum + (Number(s.km) || 0), 0), totalDep = editableShifts.reduce((sum, s) => sum + (Number(s.deplacement) || 0) + (Number(s.other_dep) || 0), 0);
  return <><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 12 }}><div style={{ fontSize: 14, fontWeight: 700, color: 'var(--brand-d)' }}>📋 Validation hebdomadaire — {employee?.name || ''} / {client?.name || 'Client'}</div><div style={{ fontSize: 11, color: 'var(--text3)' }}>{currentReview?.status ? `Statut: ${currentReview.status}` : 'Aucune approbation enregistrée'}</div></div><div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, minmax(0, 1fr))', gap: 10, marginBottom: 12 }}><div style={{ background: '#fff', borderRadius: 8, padding: '10px 14px', border: '1px solid var(--border)' }}><div style={{ fontSize: 10, color: 'var(--text3)' }}>Quarts</div><div style={{ fontSize: 16, fontWeight: 700, color: 'var(--brand)' }}>{editableShifts.length}</div></div><div style={{ background: '#fff', borderRadius: 8, padding: '10px 14px', border: '1px solid var(--border)' }}><div style={{ fontSize: 10, color: 'var(--text3)' }}>Heures affichées</div><div style={{ fontSize: 16, fontWeight: 700 }}>{plannedHours.toFixed(2)}h</div></div><div style={{ background: '#fff', borderRadius: 8, padding: '10px 14px', border: '1px solid var(--border)' }}><div style={{ fontSize: 10, color: 'var(--text3)' }}>Kilométrage</div><div style={{ fontSize: 16, fontWeight: 700 }}>{totalKm} km</div></div><div style={{ background: '#fff', borderRadius: 8, padding: '10px 14px', border: '1px solid var(--border)' }}><div style={{ fontSize: 10, color: 'var(--text3)' }}>Frais</div><div style={{ fontSize: 16, fontWeight: 700 }}>{fmtMoney(totalDep + totalKm * RATE_KM)}</div></div></div><div style={{ display: 'grid', gridTemplateColumns: '2fr 1fr', gap: 12, alignItems: 'start', marginBottom: 12 }}><div style={{ background: '#fff', borderRadius: 8, padding: 12, border: '1px solid var(--border)' }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}><div style={{ fontSize: 12, fontWeight: 700 }}>Édition rapide des quarts / frais</div><button className="btn btn-outline btn-sm" onClick={addQuickShiftRow}>+ Ajouter ligne</button></div><div style={{ display: 'grid', gridTemplateColumns: '96px 88px 88px 88px 82px 72px 90px 90px 90px 88px', gap: 6, fontSize: 10, color: 'var(--text3)', marginBottom: 6 }}><div>Date</div><div>Début</div><div>Fin</div><div>Pause min</div><div>Heures</div><div>KM</div><div>Dépl.</div><div>Autre</div><div>Notes</div><div></div></div>{editableShifts.map(s => <div key={s.id} style={{ display: 'grid', gridTemplateColumns: '96px 88px 88px 88px 82px 72px 90px 90px 90px 88px', gap: 6, marginBottom: 6, alignItems: 'center' }}><input className="input" type="date" style={{ padding: '6px 8px', fontSize: 12 }} value={s.date} onChange={e => updateEditableShift(s.id, 'date', e.target.value)} /><input className="input" type="time" style={{ padding: '6px 8px', fontSize: 12 }} value={normalizeTimeForInput(s.start)} onChange={e => updateEditableShift(s.id, 'start', e.target.value)} /><input className="input" type="time" style={{ padding: '6px 8px', fontSize: 12 }} value={normalizeTimeForInput(s.end)} onChange={e => updateEditableShift(s.id, 'end', e.target.value)} /><input className="input" type="number" step="1" style={{ padding: '6px 8px', fontSize: 12 }} value={s.pause_minutes || 0} onChange={e => updateEditableShift(s.id, 'pause_minutes', e.target.value)} /><input className="input" type="number" step="0.25" style={{ padding: '6px 8px', fontSize: 12, background: '#f8f9fa' }} value={Number(s.hours || 0).toFixed(2)} readOnly /><input className="input" type="number" step="1" style={{ padding: '6px 8px', fontSize: 12 }} value={s.km || 0} onChange={e => updateEditableShift(s.id, 'km', e.target.value)} /><input className="input" type="number" step="0.01" style={{ padding: '6px 8px', fontSize: 12 }} value={s.deplacement || 0} onChange={e => updateEditableShift(s.id, 'deplacement', e.target.value)} /><input className="input" type="number" step="0.01" style={{ padding: '6px 8px', fontSize: 12 }} value={s.other_dep || 0} onChange={e => updateEditableShift(s.id, 'other_dep', e.target.value)} /><input className="input" type="text" style={{ padding: '6px 8px', fontSize: 12 }} value={s.notes || ''} onChange={e => updateEditableShift(s.id, 'notes', e.target.value)} /><button className="btn btn-outline btn-sm" onClick={() => saveShiftLine(s)} disabled={savingShiftId === s.id}>{savingShiftId === s.id ? '...' : 'Sauver'}</button></div>)}</div><div style={{ background: '#fff', borderRadius: 8, padding: 12, border: '1px solid var(--border)' }}><div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>Résumé estimatif</div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>Services</span><strong>{fmtMoney(totals.service)}</strong></div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>KM</span><strong>{fmtMoney(totals.km)}</strong></div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>Déplacement</span><strong>{fmtMoney(totals.dep)}</strong></div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>Autres frais</span><strong>{fmtMoney(totals.autres)}</strong></div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>Hébergement</span><strong>{fmtMoney(totals.accom)}</strong></div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 13, padding: '6px 0', borderTop: '1px solid #dee2e6', marginTop: 6 }}><span>Sous-total</span><strong>{fmtMoney(totals.subtotal)}</strong></div>{!client?.tax_exempt && <><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>TPS</span><strong>{fmtMoney(totals.tps)}</strong></div><div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 12, padding: '3px 0' }}><span>TVQ</span><strong>{fmtMoney(totals.tvq)}</strong></div></>}{client?.tax_exempt && <div style={{ fontSize: 11, color: '#28A745', paddingTop: 6 }}>Client exempté de taxes</div>}<div style={{ display: 'flex', justifyContent: 'space-between', fontSize: 16, padding: '10px 12px', marginTop: 8, background: '#2A7B88', color: '#fff', borderRadius: 8 }}><span>Total estimé</span><strong>{fmtMoney(totals.total)}</strong></div></div></div><div style={{ display: 'grid', gridTemplateColumns: '1.4fr 1fr', gap: 12, marginBottom: 12 }}><div style={{ background: '#fff', borderRadius: 8, padding: 12, border: '1px solid var(--border)' }}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}><div style={{ fontSize: 12, fontWeight: 700 }}>Hébergement lié à la semaine</div><div style={{ fontSize: 10, color: 'var(--text3)' }}>{loadingAccommodations ? 'Chargement…' : `${accommodationEstimates.length} ligne(s)`}</div></div>{accommodationEstimates.length === 0 ? <div style={{ fontSize: 11, color: 'var(--text3)', marginBottom: 8 }}>Aucun hébergement trouvé pour cette semaine.</div> : <div>{accommodationEstimates.map(a => <div key={a.id} style={{ display: 'grid', gridTemplateColumns: '1.4fr .7fr .8fr .8fr', gap: 8, padding: '6px 0', borderBottom: '1px solid #f0f0f0', fontSize: 12 }}><span>{a.start_date} → {a.end_date}</span><span>{a.estimate.days} jour(s)</span><span>{fmtMoney(a.estimate.costPerDay)}/j</span><strong>{fmtMoney(a.estimate.amount)}</strong></div>)}</div>}<div style={{ borderTop: '1px solid #f0f0f0', marginTop: 8, paddingTop: 8 }}><div style={{ fontSize: 11, fontWeight: 700, marginBottom: 6 }}>Ajout rapide hébergement</div><div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr 1.2fr auto', gap: 6 }}><input className="input" type="date" style={{ padding: '6px 8px', fontSize: 12 }} value={accomForm.start_date} onChange={e => setAccomForm(f => ({ ...f, start_date: e.target.value }))} /><input className="input" type="date" style={{ padding: '6px 8px', fontSize: 12 }} value={accomForm.end_date} onChange={e => setAccomForm(f => ({ ...f, end_date: e.target.value }))} /><input className="input" type="number" step="0.01" style={{ padding: '6px 8px', fontSize: 12 }} placeholder="Coût total" value={accomForm.total_cost} onChange={e => setAccomForm(f => ({ ...f, total_cost: e.target.value }))} /><input className="input" type="text" style={{ padding: '6px 8px', fontSize: 12 }} placeholder="Notes" value={accomForm.notes} onChange={e => setAccomForm(f => ({ ...f, notes: e.target.value }))} /><button className="btn btn-outline btn-sm" onClick={saveQuickAccommodation} disabled={savingAccommodation}>{savingAccommodation ? '...' : 'Ajouter'}</button></div></div></div><div style={{ background: '#fff', borderRadius: 8, padding: 12, border: '1px solid var(--border)' }}><div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>Approbation / justificatifs</div><div style={{ display: 'grid', gridTemplateColumns: '1fr', gap: 10, marginBottom: 12 }}><div><label style={{ fontSize: 11, color: 'var(--text3)' }}>Heures approuvées</label><input className="input" type="number" step="0.25" value={reviewDraft.approvedHours} onChange={e => setReviewDraft(d => ({ ...d, approvedHours: e.target.value }))} /></div><div><label style={{ fontSize: 11, color: 'var(--text3)' }}>Notes</label><input className="input" value={reviewDraft.notes} onChange={e => setReviewDraft(d => ({ ...d, notes: e.target.value }))} placeholder="Notes de vérification" /></div></div><div style={{ display: 'flex', gap: 8, flexWrap: 'wrap', marginBottom: 12 }}><button className="btn btn-outline btn-sm" onClick={onSave}>Enregistrer</button><button className="btn btn-primary btn-sm" style={{ background: currentReview?.status === 'approved' ? '#28A745' : undefined }} onClick={onApprove} disabled={!editableShifts.length}>Approuver les heures</button><button className="btn btn-outline btn-sm" onClick={onRevoke} disabled={!currentReview}>Révoquer</button><button className="btn btn-primary btn-sm" onClick={onGenerateInvoice}>Générer la facture approuvée</button><label className="btn btn-outline btn-sm" style={{ cursor: 'pointer' }}>Ajouter justificatif<input type="file" accept=".pdf,.jpg,.jpeg,.png,.gif" style={{ display: 'none' }} onChange={onUpload} /></label>{currentInvoice && <button className="btn btn-outline btn-sm" onClick={onGoInvoices}>Voir dans Facturation</button>}</div>{currentInvoice && <div style={{ background: '#eef7ff', border: '1px solid #c7e1ff', borderRadius: 8, padding: 10, marginBottom: 12 }}><div style={{ fontSize: 11, color: 'var(--text3)' }}>Facture générée</div><div style={{ fontWeight: 700 }}>{currentInvoice.number} — {fmtMoney(currentInvoice.total || 0)}</div></div>}<div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>Justificatifs ({reviewAttachments.length})</div>{reviewAttachments.length === 0 ? <div style={{ fontSize: 11, color: 'var(--text3)' }}>Aucune pièce jointe</div> : reviewAttachments.map(att => <div key={att.id} style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: 8, padding: '6px 0', borderBottom: '1px solid #f0f0f0', fontSize: 12 }}><span>{att.filename}</span><button className="btn btn-outline btn-sm" style={{ padding: '2px 8px' }} onClick={() => onDeleteAttachment(att.id)}><Trash2 size={12} /></button></div>)}</div></div></>;
}

function ShiftPill({ shift, onClick }) { const bg = shift.status === 'draft' ? 'var(--surface2)' : shift.status === 'published' ? 'var(--brand-l)' : 'var(--green-l)'; const hasDep = shift.km || shift.deplacement || shift.autre_dep; const hasMandat = shift.mandat_start || shift.mandat_end; return <div className="shift-pill" style={{ background: bg }} onClick={onClick} title={`${shift.location}\n${shift.start}—${shift.end} (${shift.hours}h net)`}><div style={{ fontWeight: 600, fontSize: 10.5, whiteSpace: 'nowrap' }}>{normalizeTimeForInput(shift.start) || shift.start}–{normalizeTimeForInput(shift.end) || shift.end}</div><div style={{ color: 'var(--text3)', fontSize: 9 }}>{shift.hours}h{shift.pause > 0 && <span style={{ color: 'var(--amber)' }}> (−{pauseHoursToMinutes(shift.pause)} min)</span>}</div><div style={{ color: 'var(--text3)', fontSize: 9, overflow: 'hidden', textOverflow: 'ellipsis', whiteSpace: 'nowrap', maxWidth: 90 }}>{shift.location?.split(' ').slice(0, 3).join(' ')}</div>{shift.notes && <div style={{ fontSize: 8, color: 'var(--amber)', marginTop: 1 }}>✏ note</div>}{hasDep > 0 && <div style={{ fontSize: 8, color: 'var(--purple)', marginTop: 1 }}>$ dépense</div>}{hasMandat && <div style={{ fontSize: 8, color: 'var(--teal)', marginTop: 1 }}>📅 mandat</div>}</div>; }
function YearView({ schedules, employees, selectedDate }) { const y = selectedDate.getFullYear(); return <div className="schedule-grid"><table><thead><tr><th>Employé</th>{MONTHS_SHORT.map(m => <th key={m}>{m}</th>)}<th style={{ background: 'var(--brand-xl)' }}>Total</th></tr></thead><tbody>{employees.map(e => { let total = 0; return <tr key={e.id}><td><div style={{ display: 'flex', alignItems: 'center', gap: 6 }}><Avatar name={e.name} size={26} /><div style={{ fontWeight: 600, fontSize: 11 }}>{e.name}</div></div></td>{Array.from({ length: 12 }, (_, m) => { const prefix = `${y}-${String(m + 1).padStart(2, '0')}`; const hrs = schedules.filter(s => s.employee_id === e.id && s.date.startsWith(prefix)).reduce((sum, s) => sum + s.hours, 0); total += hrs; return <td key={m} style={{ fontSize: 11 }}>{hrs ? hrs.toFixed(1) : '—'}</td>; })}<td style={{ fontSize: 12, fontWeight: 700, color: 'var(--brand)', background: 'var(--brand-xl)' }}>{total.toFixed(1)}h</td></tr>; })}</tbody></table></div>; }
