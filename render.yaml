# render.yaml — Render.com Blueprint
# Deploy with: render blueprint sync

services:
  # Backend API
  - type: web
    name: soins-expert-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: soins-expert-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: FRONTEND_URL
        value: https://soins-expert-plus.com
      - key: ANTHROPIC_API_KEY
        sync: false  # Set manually in Render dashboard
      - key: OPENAI_API_KEY
        sync: false  # Set manually in Render dashboard
      - key: OPENAI_MODEL
        value: gpt-5.4-mini
      - key: OPENAI_REASONING_EFFORT
        value: medium
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        value: paie@soins-expert-plus.com
      - key: SMTP_PASS
        sync: false  # Set manually
      - key: BILLING_SENDER_EMAIL
        value: paie@soins-expert-plus.com
      - key: BILLING_EMAIL_TRANSPORT
        value: smtp
      - key: BILLING_GMAIL_CLIENT_ID
        sync: false
      - key: BILLING_GMAIL_CLIENT_SECRET
        sync: false
      - key: BACKEND_PUBLIC_URL
        sync: false
    healthCheckPath: /api/health

  # Frontend (static site)
  - type: web
    name: soins-expert-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://soins-expert-api.onrender.com/api
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: soins-expert-db
    databaseName: soins_expert
    plan: free  # Upgrade to starter ($7/mo) for production
